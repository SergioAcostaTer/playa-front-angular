import { Routes } from '@angular/router';
import { BeachDetailComponent } from './beach-detail/beach-detail.component';

export const routes: Routes = [
    {path: '', component: BeachDetailComponent}
];
